<%= form_for(@shipping_line_office) do |f| %>
  <% if @shipping_line_office.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shipping_line_office.errors.count, "error") %> prohibited this shipping_line_office from being saved:</h2>

      <ul>
      <% @shipping_line_office.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :area_address %><br />
    <%= f.text_field :area_address %>
  </div>
  <div class="field">
    <%= f.label :shipping_line_id %><br />
    <%= collection_select (:shipping_line_office, :shipping_line_id, ShippingLine.all, :id, :company_name) %>
  </div>
  <div class="field">
    <%= f.label :port_id %><br />
    <%= collection_select (:shipping_line_office, :port_id, Port.all, :id, :port_name) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
